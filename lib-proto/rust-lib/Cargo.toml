[package]
name = "person-proto-lib"
version = "1.0.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
prost = "0.12"
prost-types = "0.12"

# uniquement si tu veux aussi encoder/décoder depuis ce crate
bytes = "1"
